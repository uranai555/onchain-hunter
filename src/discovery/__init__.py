"""Discovery package — 0→1 wallet discovery surfaces.

This package implements the wallet discovery strategy from
onchain_wallet_discovery_0_to_1_strategy_v1.md.

Current MVP surfaces:
  - Event winners mining (existing candidate wallets)
  - (future) Fresh profitable trader detection
  - (future) Token early buyer winners
  - (future) Yield migration wallets
"""